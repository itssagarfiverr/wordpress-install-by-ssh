

# Update the package list
sudo apt update -y
sudo apt install apache2 -y
sudo systemctl enable apache2
sudo systemctl start apache2
sudo apt install certbot python3-certbot-apache -y


# Download direct_wp.sh script and place it in the /intechost/cloud directory
mkdir -p /intechost/cloud
curl -o /intechost/cloud/direct_wp.sh https://intecdev.com/bash/direct_wp.sh

# Disable other MOTD scripts
sudo chmod -x /etc/update-motd.d/*

# Ensure the setup script is executable and then run it
chmod +x /intechost/cloud/direct_wp.sh
/intechost/cloud/direct_wp.sh
