

mkdir -p /intechost/aapanel
curl -o /intechost/aapanel/aapanel.sh https://raw.githubusercontent.com/itssagarfiverr/wordpress-install-by-ssh/main/aaPanel.sh

# Disable other MOTD scripts
sudo chmod -x /etc/update-motd.d/*

# Ensure the setup script is executable and then run it
chmod +x /intechost/aapanel/aapanel.sh
/intechost/aapanel/aapanel.sh
